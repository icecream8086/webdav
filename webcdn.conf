# /etc/nginx/conf.d/webcdn.conf
server {
    listen 5500;
    server_name _;
    
    # 设置webdav目录 ! 确保文件存在
    root /data/nginx-upload-storage;
    
    # 不限制文件大小
    client_max_body_size 0;
    
    # CORS 头
    add_header Access-Control-Allow-Origin "*" always;
    add_header Access-Control-Allow-Methods "GET, PUT, DELETE, MKCOL, COPY, MOVE, OPTIONS, PROPFIND, LOCK, UNLOCK" always;
    add_header Access-Control-Allow-Headers "*" always;
    
    # 主 location - 处理所有WebDAV请求
    location / {
        autoindex on;
        autoindex_localtime on;
        autoindex_exact_size off;
        
        # 客户端请求体临时路径
        client_body_temp_path /tmp;
        
        # 为目录URI后加上斜杠，解决客户端兼容性问题
        set $dest $http_destination;
        if (-d $request_filename) {
            rewrite ^(.*[^/])$ $1/;
            set $dest $dest/;
        }

        if ($request_method ~ (MOVE|COPY)) {
            # 对MOVE|COPY方法强制添加Destination请求头
            # 注意：这里使用add_header，因为more_set_input_headers需要额外模块
            add_header Destination $dest;
        }

        if ($request_method ~ MKCOL) {
            rewrite ^(.*[^/])$ $1/ break;
        }

        # DAV支持的请求方法
        dav_methods PUT DELETE MKCOL COPY MOVE;
        
        # DAV扩展支持的请求方法 (需要nginx-dav-ext-module)
        # dav_ext_methods PROPFIND OPTIONS LOCK UNLOCK;
        
        # DAV扩展锁绑定的内存区域 (需要nginx-dav-ext-module)
        # dav_ext_lock zone=davlock;
        
        # 启用创建目录支持 - 关键指令
        create_full_put_path on;
        
        # 设置创建的文件及目录的访问权限
        dav_access user:rw group:r all:r;
        
        # OPTIONS 预检
        if ($request_method = 'OPTIONS') {
            return 204;
        }
        
        # 不要直接返回PUT响应，让WebDAV处理文件保存
        # WebDAV会自动返回适当的响应
    }
    
    # 健康检查
    location /health {
        return 200 'healthy';
    }
}
